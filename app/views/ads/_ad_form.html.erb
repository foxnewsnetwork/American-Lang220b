<%= form_for @ad, :html => { :multipart => true } do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>

      <div class = "field">
        <%= f.file_field :photo %>
      </div>
    <p> <%= f.submit "Submit" %></p>
<% end %>
